# Docker container - gcc

A simple container which has gcc installed. Basics ...

## How to build
```
docker build -t ubuntu-gcc .
```

## Start the container
```
docker run -it --rm --name ubuntu-gcc   ubuntu-gcc
```


